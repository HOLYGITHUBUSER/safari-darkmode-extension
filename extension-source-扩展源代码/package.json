{
  "name": "safari-darkmode-extension",
  "version": "2.2.0",
  "private": true,
  "description": "Safari 全局暗黑模式扩展（TypeScript 源码）",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -f src/*.js src/*.js.map popup/popup.js popup/popup.js.map",
    "zip": "npm run build && cd .. && rm -f SafariDarkMode-Extension.zip && zip -r SafariDarkMode-Extension.zip extension-source-扩展源代码 -x '*/node_modules/*' '*/.DS_Store' '*/*.ts' '*/tsconfig.json'",
    "test": "playwright test",
    "test:visual": "playwright test --visual",
    "test:ui": "playwright test --ui",
    "test:install": "playwright install"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.0.0",
    "@playwright/test": "^1.59.1",
    "playwright": "^1.59.1",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
